{
  "name": "nuxt-extend-nuxt-forms",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf .nuxt && rm -rf .output",
    "reinstall": "rm -rf node_modules && rm ./package-lock.json && npm install",
    "cleaninstall": "npm run clean && npm run reinstall",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/icon": "1.7.5",
    "modern-normalize": "3.0.1",
    "nuxt": "3.14.159",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@nuxt/eslint-config": "0.7.0",
    "eslint": "9.15.0",
    "srcdev-nuxt-components": "0.0.4",
    "srcdev-nuxt-forms": "^2.1.12",
    "vue": "3.5.13"
  }
}
